# App admin (Vite) — copy sang .env.local để chạy dev/build.
# URL server game (control plane) mà app admin gọi tới. Mặc định server local.
# Production: trỏ tới origin server thật (vd https://api.your-domain).
VITE_API_URL=http://localhost:8910
