# Flat node_modules (giống npm) để tránh lỗi peer-dep của Next/Three/Nest trong pnpm.
node-linker=hoisted
# pnpm 11 chạy install trước mỗi "pnpm run" → tắt để script (tsc/vitest) chạy trực tiếp,
# không bị escalate cảnh báo ignored-builds thành lỗi.
verify-deps-before-run=false
